package com.hybridFramework.report;

import java.util.Calendar;

import org.openqa.selenium.By;
import org.testng.annotations.Test;

import com.hybridFramework.helper.AlertHelper;
import com.hybridFramework.testBase.TestBase;


public class Sample extends TestBase {

	@Test
	public void sample() throws InterruptedException
	{
		/*String name=" (132432)".trim();
		String number="";
		for(int i=1;i<name.length()-1;i++)
		{
			number=number+name.charAt(i);
		}
		System.out.println(number);*/
		
		/*//Test Alert
		driver.get("file:///D:/Study%20Materials/Selenium/WebPage/Alert.html");
		driver.findElement(By.xpath("//button[text()='Try it']")).click();
		AlertHelper alert=new AlertHelper(driver);
		alert.sendKeyIfAlertPresent("Syed");*/
	}
}
